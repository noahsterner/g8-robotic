[project]
name = "xyz-robotics"
version = "0.0.0"
description = ""
readme = "README.md"
license = { file = "LICENSE" }
dependencies = [
    "bluezero>=0.5.0"
]

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
pythonpath = [ "src" ]
testpaths = [ "tests" ]
